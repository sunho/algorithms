package main

import "fmt"

func QuickSort(arr []int, lo int, ro int) {
	if lo >= ro {
		return
	}
	piv := arr[0]
	l := lo + 1
	r := ro
	for l < r{
		for arr[l] < piv || 
			l ++
		}
	}
}

func main() {
	fmt.Println("vim-go")
}
