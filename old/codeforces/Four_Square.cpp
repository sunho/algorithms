#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
  cin.tie(nullptr)->sync_with_stdio(false);

  vector<pair<int,int>> a(4);
  for (int i=0;i<4;i++) {
    cin >> a[i].first >> a[i].second;
  }
  auto combine = [&]()
  auto dfs = [&](auto self, int i, bool rotated) {
    if (rotated) {
      
    }
  };
}
